export const enum ConfigurationType {
  TextConfiguration,
  ExcelConfiguration,
  MdbConfiguration,
}

export const enum LogType {
  Info,
  Warning,
  Error
}

export enum BranchStatus{
  Acitve,
  Closing,
  Inactive,
  Closed
}